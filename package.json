{
  "name": "pokemon-price-lens",
  "version": "0.4.0",
  "private": true,
  "description": "A dependency-free Chrome extension for comparing English and Japanese Pokémon card prices.",
  "repository": {
    "type": "git",
    "url": "https://github.com/ddxu1/pokemon-price-lens.git"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check navigation.js && node --check card-detector.js && node --check content.js && node --check pricecharting.js && node --check pricecharting-content.js && node --test tests/*.test.js"
  }
}
